# Core web app
Flask>=3.0
Flask-Login>=0.6
python-dotenv>=1.0

# Google APIs (Gmail + Calendar)
google-api-python-client>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Email parsing
beautifulsoup4>=4.12
lxml>=5.0
datefinder>=0.7

# Maps / routing
googlemaps>=4.10

# NLP / NER
spacy>=3.7
spacy-transformers>=1.3

# Data handling
pandas>=2.0

# Database (MariaDB / MySQL)
mysql-connector-python>=8.0

# Invoicing automation
selenium>=4.0

# Health check
requests>=2.31

# Note: datetime, http.client and os are part of the Python standard library
# and must NOT be listed as dependencies (the original requirements.txt did).
